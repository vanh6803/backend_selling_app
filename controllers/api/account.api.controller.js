const accountModel = require("../../models/account.model");

exports.detailProfile = async (req, res, next) => {
    try {
        var user = await accountModel.account
    } catch (error) {
        
    }
}